server:
  port: 5000
spring:
  application:
    name: item-service
  profiles:
    active: local
  cloud:
    config:
      uri: http://172.19.0.3:8080
    loadbalancer:
      ribbon:
        enabled: false